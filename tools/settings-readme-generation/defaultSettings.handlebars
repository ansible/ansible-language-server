The following are the default values of the settings provided by the Ansible
Language Server:

{{#each arrayOfDefaultSettings}}
  - **{{setting}}**:
  {{description}}<br />_default value: `{{defaultValue}}`_

{{/each}}